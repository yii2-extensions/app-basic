name: Docker CI

on:
  pull_request:
    paths:
      - 'docker/**'
      - 'docker-compose*.yml'
      - '.github/workflows/docker.yml'
  push:
    paths:
      - 'docker/**'
      - 'docker-compose*.yml'
      - '.github/workflows/docker.yml'

jobs:
  test-apache:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout.
        uses: actions/checkout@v4

      - name: Install docker compose.
        run: |
          sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          sudo chmod +x /usr/local/bin/docker-compose
          docker-compose --version

      - name: Build and start containers.
        run: docker-compose build --no-cache && docker-compose up -d

      - name: Update vendor packages.
        run: docker exec yii2-apache composer update --prefer-dist -vvv

      - name: Run codeception build and tests.
        run: |
          docker exec yii2-nginx vendor/bin/codecept build
          docker exec yii2-nginx vendor/bin/codecept run

  test-nginx:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout.
        uses: actions/checkout@v4

      - name: Install docker Compose.
        run: |
          sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          sudo chmod +x /usr/local/bin/docker-compose
          docker-compose --version

      - name: Build and start containers.
        run: docker-compose -f docker-compose build --no-cache && docker-compose -f docker-compose-nginx.yml up -d

      - name: Update vendor packages.
        run: docker exec yii2-nginx composer update --prefer-dist -vvv

      - name: Run codeception build and tests.
        run: |
          docker exec yii2-nginx vendor/bin/codecept build
          docker exec yii2-nginx vendor/bin/codecept run
